
revoir l'arborescence :
   GSH_ROOT
       # statiques
       bin
       missions
       lib

       # dynamiques
       session_data
       tmp
       config
       World

tout exécuter depuis l'archive (ne pas recopier les fichiers vers une autre
destination)

petit utilitaire pour générer une archive adéquate

mode debug :
  - diff entre les environnements avant / après
  - pas de mot de passe admin
  - creation du coffre automatiquement

gsh auto : utilise un fichier "auto.sh" pour valider la mission

mot de passe demandé "dans" les fonctions _gsh_...

vérifier si la commande ldapsearch est présente...

"check requirements" lors de l'install

unset -f check  /  unset -f init  automatique après les "source check.sh" / source init.sh"

BUG si 2 missions ont le même numéro

améliorer la gestion du coffre pour les scripts "check" :
  - par la fonction _coffre : ajout de scripts "statiques" dans les missions ?
  - ajout d'une définition GSH_COFFRE GSH_CABANE

vérifier la présence du coffre dans les missions ?



gsh replay <speed>, avec un script par mission
  => compliqué : ttyrec relance un shell à chaque fois, on perd donc certains
  trucs (alias, etc.)

========

ajouter d'autres bonus : couleurs dans le prompt, GREP_OPTION, ...

