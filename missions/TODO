ls -l
ls *   vs  ls -d *
less / more, redirections
PATH
liens symboliques
sort / uniq
C-r pour rechercher dans l'historique
tr
un peu plus de motifs
expressions régulières et grep
touch pour créer des fichiers vide / mettre la date à jours
script: boucles sur les fichiers
gestion de historique
pushd popd
wget / curl
tar / zip



rq

``bind 'set disable-completion on'`` pour désactiver la completion,  ``bind ... off`` pour la réactiver
