Mission goal
============

Build a "Hut" in the forest, and then build a "Chest" in the 
hut.

Remark
------

The chest must be **in** the hut.


Useful commands
===============

mkdir DIRECTORY
  Create a new directory inside the current directory.
  Remark: ``mkdir`` is an abbreviation for "make directory".
