Mission goal
============

Build a "Cabin" in the forest, and the build a "Chest" in the cabin.

NOTE: the chest must be **in** the cabin.


Useful commands
===============

mkdir DIRECTORY
  create a new directory in the current directory
