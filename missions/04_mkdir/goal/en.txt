Mission goal
============

Build a "Cabin" in the forest, and the build a "Chest" 
in the cabin.

NOTE: the chest must be **in** the cabin.


Useful commands
===============

mkdir DIRNAME
  Create a new directory called DIRNAME inside the 
  current directory.
