Mission goal
============

Build a "Cabin" in the forest, and the build a "Chest"
in the cabin.

Be careful, the chest must be IN then cabin.


Useful commands
===============

mkdir DIRECTORY
  creates a new directory in the current directory
