Mission goal
============

Build a "Cabin" in the forest, and then build a "Chest" in the 
cabin.

Remark
------

The chest must be **in** the cabin.


Useful commands
===============

mkdir DIRECTORY
  Create a new directory inside the current directory.
  Remark: ``mkdir`` is an abbreviation for "make directory".
