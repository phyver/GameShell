Mission goal
============

Now merge the add_rune branch in the main branch. Take care on the branch your are on currently, maybe you should change !


Useful commands
===============

To merge a branch. Ensure you are on the branch you want to merge into and then
git merge BRANCH_NAME

If you end up into a CONFLICT and would like to abort the current merge operation
git merge --abort


