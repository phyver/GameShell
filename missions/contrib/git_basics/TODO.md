  - [x] `git config user.name` (00_config, old name 02_configGitID)
  - [x] `git init` a new repo (01_init, new)
  - [ ] cloning a repo => requires clarifying how to deal with remotes
  - [x] creating a file that is not yet added (02_create_file, old name 02a_...)
  - [x] adding a new file (03_add, old name 02b_...)
  - [x] commiting a new file (04_commit, old name 02c_...)
  - [ ] adding changes to a tracked file (new)
  - [ ] `git diff` and possibly variants `git diff --staged` and `git diff HEAD` (new)
  - [ ] .gitignore file
  - [ ] `git checkout OLD_HASH` (old name 05[ab]_...)


The next missions could be moved to git_misc?

  - [ ] `git mv` (old name 02_git_mv)  =>  should there be something about `mv
SOURCE DEST FILE ; git add SOURCE`?
  - [ ] `git rm` (old name 04_git_rm)  =>  should there be something about `rm
FILE; git add FILE`, possibly in another mission?


