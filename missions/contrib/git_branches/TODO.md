The order of the following missions is not yet clear

  - [ ] `git switch -c` to create a new local branch
  - [ ] `git switch ...` in order to get a distant branch  =>  check it works with local remotes
  - [ ] `git branch`
  - [ ] several missions about merging, conflicts, etc. to be refined from existing missions in gameshell_git
  - [ ] ??? mission(s) about `git rebase` and associated problems ???


The following could go in git_remotes

  - [ ] `git branch -r` to get list of branches
  - [ ] `git branch -vv` to see associated commits and remotes
